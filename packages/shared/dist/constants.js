/** Local send time for daily email receipts (HH:mm, 24h). */
export const DEFAULT_DAILY_RECEIPT_TIME = '18:00';
/** Cron runs every N minutes; match send window [target, target + window). */
export const RECEIPT_SEND_WINDOW_MINUTES = 5;
